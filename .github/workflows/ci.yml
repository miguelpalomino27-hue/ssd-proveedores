name: CI - Pruebas del SSD de Proveedores

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:

  # =========================================
  # 🚫 BACKEND DESACTIVADO TEMPORALMENTE
  # =========================================
  # test-backend:
  #   runs-on: ubuntu-latest
  #
  #   services:
  #     mysql:
  #       image: mysql:8.0
  #       env:
  #         MYSQL_ROOT_PASSWORD: root_ci_password
  #         MYSQL_DATABASE: ssd_proveedores
  #         MYSQL_USER: ssd_app
  #         MYSQL_PASSWORD: SsdAppCI2026
  #       ports:
  #         - 3306:3306
  #       options: >-
  #         --health-cmd="mysqladmin ping"
  #         --health-interval=10s
  #         --health-timeout=5s
  #         --health-retries=5
  #
  #   steps:
  #     - name: Clonar repositorio
  #       uses: actions/checkout@v4
  #
  #     - name: Configurar Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #         cache-dependency-path: backend/package-lock.json
  #
  #     - name: Instalar dependencias del backend
  #       working-directory: ./backend
  #       run: npm ci
  #
  #     - name: Cargar esquema y datos semilla
  #       run: |
  #         mysql -h127.0.0.1 -uroot -proot_ci_password < database/schema.sql
  #         mysql -h127.0.0.1 -uroot -proot_ci_password < database/seed.sql
  #
  #     - name: Ejecutar pruebas unitarias e integracion
  #       working-directory: ./backend
  #       env:
  #         DB_HOST: 127.0.0.1
  #         DB_PORT: 3306
  #         DB_USER: ssd_app
  #         DB_PASSWORD: SsdAppCI2026
  #         DB_NAME: ssd_proveedores
  #         JWT_SECRET: clave_ci_pipeline
  #       run: npm test -- --coverage
  #
  #     - name: Publicar reporte de cobertura
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: cobertura-backend
  #         path: backend/coverage


  # =========================================
  # ✅ FRONTEND ACTIVO
  # =========================================
  build-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias del frontend
        working-directory: ./frontend
        run: npm ci

      - name: Compilar build de producción
        working-directory: ./frontend
        run: npm run build